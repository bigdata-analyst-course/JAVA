중첩 클래스:
	- 클래스 내부에 선언한 클래스로 두개의 클래스의 멤버들을 중첩시킴
	- 클래스간 접근 간편하게 만들며 불필요한 관계 클래스를 감춤으로써 코드 복잡성을 줄임
	
	
	선언 위치에 따른 분류		선언 위치	설명
	멤버 클래스	인스턴스	Class A{	A 객체를 생성해야만 사용 할 수 있는 B 중첩 클래스
		멤버 클래스	        Class B {} 
			}
		정적	Class A{	A 클래스로 바로 접근 할 수 있는 B 중첩 클래스
		멤버 클래스	        Static Class B {} 
			}
	로컬 클래스		Class A{	Method()가 실행될때만 사용할 수 있는 B 중첩 래스
			        Void method(){
			        Class B {} 
			        }
			}
	
	멤버 클래스:
		- 인스턴스 멤버 클래스: static 키워드 없이 선언된 클래스로 인스턴스 필드와 메소드만 선언 가능하고 그외 정적 필드와 메소드는 선언 불가능
		- 정적 멤버 클래스: static 키워드로 선언되 클래스로 모든 종류의 필드와 메소드 선언이 가능함
		- 로컬 클래스: 접근 제한자를 붙일 수 없는 중첩 클래스로 선언 제한은 인스턴스 멤버 클래스와 같음

중첩 인터페이스:
	- 클래스 멤버로 선언된 인터페이스로 해당 클래스와 긴밀한 관계를 맺는 구현 클래스 생성 목적

익명 객체:
	- 이름이 없는 객체로 단독 생성이 불가능하고 클래스 상속 혹은 인터페이스 생성 시에만 생성 가능
	- 이는 필드/로컬변수의 초기값 혹은 매개 변수의 매개 값으로 주로 대입됨
	
---------------------원지영---------------------------
