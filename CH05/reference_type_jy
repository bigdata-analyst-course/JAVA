자바 데이터 타입:
	기본 타입
		- 정수, 실수, 문자, 논리 타입을 저장하는 타입
		- 실제 값이 스택 영역 속 변수 안에 저장됨
	참조 타입: 
		- 객체(object)의 번지를 참조하는 배열, 열거, 클래스, 인터페이스 타입
		- 번지를 통해 객체를 참고하기 떄문에 변수는 스택 영역에 생성되지만 실제 값은 힙 영역에 저장됨

메모리: 메소드, 힙 영역, 스레드 ( JVM 스택) 
	메소드
		- 상수풀, 필드, 데이터, 메소드, 메소드 코드, 생성자를 포함
		- JVM 실핼시 생성되어 모든 스레드가 공유됨
	
	힙 영역:
		- 참조 타입의 객체와 배열이 생성되는 영역
		- 생성된 객체와 배열은 JVM 스택 영역의 변수나 다른 객체의 필드 참조 
	
	JVM 스택 영역
		- 각 스레드 마다 각각 존재하며 스레드 시작될때 자동 할당
		- 메소드 호출시 프레임 추가(push)하고 메소드 종료되면 해당 프레임 제거(pop)



null과 NullpointException
		- null: 참조 타입 변수가 객체를 참조 하지 않을때 할당됨
		- null값을 가지고 있는 참조 변수 타입 변수 사용시 발생 

String 타입:
		- 참조 타입 변수
		- 문자열 리터럴이 동일하다면 String 객체 공유 
		- new 연산자 사용시 동일 문자열 리터럴도 다른 객체 참조

배열 타입:
		- 같은 타입의 데이터를 인덱스 할당해 하나의 변수에 연속적인 데이터 할당이 가능한 자료구조

열거타입: 
		- 몇 개의 열거 상수 중에서 하나의 상수를 저장하는 데이터 타입 
